import RoomCard from '../../RoomCard';
import SearchBar from '../../SearchBar';
import styles from './Main.module.scss';

const Main = () => {
  return (
    <div>
      <SearchBar />
      <RoomCard />
    </div>
  );
};

export default Main;
